# ---- SERVER ----
PORT=4000
JWT_SECRET=dev_super_secret_change_me
DB_FILE=./data.db

# Admin bootstrap: first run will ensure an admin exists with these credentials.
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=Admin#12345

# Demo trading balance seeded for each new account
INITIAL_BALANCE=10000

# CORS (comma-separated origins for prod)
CORS_ORIGINS=http://localhost:5173

# ---- CLIENT ----
# Nothing needed — Vite dev server picks PORT automatically
